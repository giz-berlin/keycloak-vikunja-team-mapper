default:
  image: ${CI_DEPENDENCY_PROXY_GROUP_IMAGE_PREFIX}/gradle:9.4-jdk21

stages:
  - build

build-jar:
  stage: build
  script:
    - gradle jar
  artifacts:
    paths:
      - build/libs/*.jar
    expire_in: 1 month
